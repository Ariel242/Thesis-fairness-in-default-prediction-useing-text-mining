# Lexicon Structured Ablation — Results Report

Generated by `07_lexicon_structured_ablation.py`. Compares two arms across 14 walk-forward
fold(s), 3 algorithms: **Structured** (baseline, 126 features) vs
**Structured+Lexicon** (baseline + the 7 frozen Distress Lexicon features from
`Dictionarys/output/lexicon_features.csv`).

## Methodology

- Walk-forward, expanding window: `MIN_TRAIN_MONTHS=8`, `STEP_MONTHS=3`,
  `MIN_TEST_DEFAULTS=100` — identical protocol to `analysis/BASELINE.py`,
  `TF-IDF/tfidf_structured_ablation.py`, and `FinBERT/finbert_structured_ablation.py`, verified to
  yield exactly 14 folds.
- The lexicon is **frozen** (built once from the initial training window, per
  `01_extract_frozen_vocabulary.py` / `02_llm_zero_shot_annotate.py`) — the same 7 feature columns
  are used unchanged in every fold; only the per-fold imputer/scaler is refit on that fold's
  training rows, exactly as for the Structured block.
- **Hyperparameters are fixed, not tuned per arm** — reused verbatim from `analysis/BASELINE.py`:
  `Logistic(C=0.3, penalty=l2, solver=saga, class_weight=balanced)`,
  `RandomForest(n_estimators=500, min_samples_leaf=20, class_weight=balanced)`,
  `XGBoost(n_estimators=300, max_depth=4, learning_rate=0.05, subsample=0.8, scale_pos_weight=neg/pos per fold)`.
  All three share `random_state=242`.
- Bootstrap 95% CIs: 500 resamples, seed=42. DeLong (1988) pairwise test for
  correlated AUCs on the same test set, Structured vs Structured+Lexicon, per model, per fold.

## A. Predictive performance — best arm per model (mean AUC across folds)

- **Logistic**: best mean AUC = 0.6939 (Structured+Lexicon)
- **XGBoost**: best mean AUC = 0.6913 (Structured+Lexicon)
- **RandomForest**: best mean AUC = 0.6895 (Structured+Lexicon)

Full per-arm/per-model mean±std table: `results/lexicon_ablation_summary.csv`.

## B. DeLong pairwise significance (mean ΔAUC, fraction of folds significant)

- **Logistic**: mean ΔAUC (Structured+Lexicon − Structured) = +0.0009, significant (DeLong p<0.05) in 2/14 folds
- **XGBoost**: mean ΔAUC (Structured+Lexicon − Structured) = +0.0009, significant (DeLong p<0.05) in 0/14 folds
- **RandomForest**: mean ΔAUC (Structured+Lexicon − Structured) = +0.0014, significant (DeLong p<0.05) in 1/14 folds

Full per-fold DeLong results: `results/lexicon_ablation_delong.csv`.

## C. Lexicon feature importance ranking (mean rank among all features, lower = more important)

**Logistic**: `distress_debt_pressure_count` (mean rank 27.1), `distress_medical_life_shock_count` (mean rank 31.6), `risk_word_count` (mean rank 41.1), `risk_word_density` (mean rank 61.0), `distress_liquidity_shortage_count` (mean rank 78.4), `risk_word_flag` (mean rank 81.6), `distress_refinancing_burden_count` (mean rank 86.7)

**XGBoost**: `distress_debt_pressure_count` (mean rank 12.4), `risk_word_count` (mean rank 34.6), `risk_word_density` (mean rank 46.5), `distress_medical_life_shock_count` (mean rank 56.3), `distress_refinancing_burden_count` (mean rank 67.7), `distress_liquidity_shortage_count` (mean rank 75.7), `risk_word_flag` (mean rank 96.6)

**RandomForest**: `risk_word_density` (mean rank 17.1), `risk_word_count` (mean rank 23.6), `distress_debt_pressure_count` (mean rank 24.5), `risk_word_flag` (mean rank 36.6), `distress_liquidity_shortage_count` (mean rank 38.0), `distress_medical_life_shock_count` (mean rank 42.3), `distress_refinancing_burden_count` (mean rank 67.8)

Full table: `results/lexicon_ablation_feature_importance.csv`.

## Conclusions

This section is generated from the run's own numbers above (section A/B) — re-run the script to
refresh it if the underlying data or parameters change.

- **Logistic**: the Distress Lexicon improves on Structured (ΔAUC=+0.0009, significant in 2/14 folds).
- **XGBoost**: the Distress Lexicon improves on Structured (ΔAUC=+0.0009, significant in 0/14 folds).
- **RandomForest**: the Distress Lexicon improves on Structured (ΔAUC=+0.0014, significant in 1/14 folds).
